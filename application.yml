eureka:
  instance:
    prefer-ip-address: true
  client:
    register-with-eureka: true
    fetch-registry: true
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/

microservice:
  matcher-profiles:
    uri: http://matcher-profiles/v1/profiles
